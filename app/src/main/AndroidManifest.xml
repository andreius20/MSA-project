<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MSA"
        tools:targetApi="31">

        <!-- Include your additional activities here -->
        <activity
            android:name="com.example.msa.ui.ReteteleMeleActivity"
            android:label="Retetele Mele Activity">
            <!-- Intent filter, if needed -->
        </activity>

        <activity
            android:name="com.example.msa.ui.FavoriteActivity"
            android:label="Favorite Activity">
            <!-- Intent filter, if needed -->
        </activity>

        <activity
            android:name="com.example.msa.ui.DescoperaActivity"
            android:label="Descopera Activity">
            <!-- Intent filter, if needed -->
        </activity>

        <activity
            android:name="com.example.msa.ui.RetetaNouaActivity"
            android:label="Reteta Noua Activity">
            <!-- Intent filter, if needed -->
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MSA.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
